source ../.env
rm -f sym/* obj/* Oberon.Log
rm -f ORP ORTool ORX ORL ORL32
if [ $# -ne 0 ];
then
  echo "clean"
  exit 0
fi
###
BINS="ORP ORTool ORX ORL"
if [ $O2CARCH -eq 32 ];
then
  BINS="$BINS ORL32"
fi
for m in $BINS
do
    rm -f $m
    o2c -M $m | o2ef
done
